on:
  push:
    branches: [main]

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install deps
        run: pip install -r src/requirements.txt

      - name: Run tests
        run: pytest tests/ -q   # pipeline stops here if tests fail
